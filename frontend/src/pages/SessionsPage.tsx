import { AppShell } from "../app/AppShell";

export function SessionsPage() {
  return (
    <AppShell statusText="Sessions workspace placeholder">
      <section className="panel">
        <h2>Sessions Workspace</h2>
        <p className="muted">Placeholder page for upcoming sessions architecture.</p>
      </section>
    </AppShell>
  );
}
