import { AppShell } from "../app/AppShell";

export function EventsPage() {
  return (
    <AppShell statusText="Events workspace placeholder">
      <section className="panel">
        <h2>Events Workspace</h2>
        <p className="muted">Placeholder page for upcoming events architecture.</p>
      </section>
    </AppShell>
  );
}
