import { AppShell } from "../app/AppShell";

export function RegistryPage() {
  return (
    <AppShell statusText="Registry workspace placeholder">
      <section className="panel">
        <h2>Registry Workspace</h2>
        <p className="muted">Placeholder page for upcoming registry architecture.</p>
      </section>
    </AppShell>
  );
}
